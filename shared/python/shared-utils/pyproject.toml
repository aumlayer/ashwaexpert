[project]
name = "ashva-shared-utils"
version = "0.1.0"
description = "Shared Python utilities for Ashva microservices."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}
