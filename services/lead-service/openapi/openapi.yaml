openapi: 3.0.3
info:
  title: lead-service
  version: 0.1.0
paths:
  /api/v1/leads:
    post:
      summary: Public create lead (triggers notifications best-effort)
      responses:
        "201": { description: Created }
