apiVersion: apps/v1
kind: Deployment
metadata:
  name: gateway-service
  namespace: ashva
spec:
  replicas: 1
  selector:
    matchLabels:
      app: gateway-service
  template:
    metadata:
      labels:
        app: gateway-service
    spec:
      containers:
        - name: gateway-service
          image: ashva/gateway-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ashva-config
            - secretRef:
                name: ashva-secrets
          env:
            - name: SERVICE_NAME
              value: gateway-service
            - name: SERVICE_PORT
              value: "8000"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /live
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: ashva
spec:
  selector:
    app: gateway-service
  ports:
    - name: http
      port: 80
      targetPort: 8000
