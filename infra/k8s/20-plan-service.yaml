apiVersion: apps/v1
kind: Deployment
metadata:
  name: plan-service
  namespace: ashva
spec:
  replicas: 1
  selector:
    matchLabels:
      app: plan-service
  template:
    metadata:
      labels:
        app: plan-service
    spec:
      containers:
        - name: plan-service
          image: ashva/plan-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: ashva-config
            - secretRef:
                name: ashva-secrets
          env:
            - name: SERVICE_NAME
              value: plan-service
            - name: SERVICE_PORT
              value: "8000"
          readinessProbe:
            httpGet:
              path: /ready
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /live
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 20
          resources:
            requests:
              cpu: "50m"
              memory: "128Mi"
            limits:
              cpu: "250m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: plan-service
  namespace: ashva
spec:
  selector:
    app: plan-service
  ports:
    - name: http
      port: 8000
      targetPort: 8000
